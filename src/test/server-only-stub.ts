// Empty stub: `import 'server-only'` is a Next.js compile-time runtime guard
// (no runtime exports). Under vitest we alias it to this no-op so Vite can
// resolve the bare specifier — see vitest.config.ts.
export {}
